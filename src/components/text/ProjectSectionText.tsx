import type { ReactChildren } from "@types"

const ProjectSectionText = ({ children }: ReactChildren) => {
  return <p className="mt-6 text-xl mb-2">{children}</p>
}
export default ProjectSectionText
